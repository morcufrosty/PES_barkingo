const {createUser,
    loginUser,
    renewGoogleToken,
    renewFacebookToken} = require('./login_register');

module.exports = {
    createUser,
    loginUser,
    renewGoogleToken,
    renewFacebookToken,
};
